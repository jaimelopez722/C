#include <stdio.h>
#include <math.h>
int main()
{
    int x = 9;
    float pi = 3.14;
    int y = -3;
    float z = 3; 

    //x = sqrt(x);
    //x = pow(x,3);
    //pi = round(pi);
    //pi = ceil(pi);
    //pi = floor(pi);
    //pi = abs(pi);
    //y = abs(y);
    //z = log(z);
    z = sin(z);

    // printf("%d\n", x);
    // printf("%.1f\n", pi);
    //printf("%d", y);
    printf("%f", z);

    return 0;
}