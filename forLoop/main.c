#include <stdio.h>

int main(){

    // for loop = Repeat some code a limited # of times
    // syntax = for(Initialization; Condition; Update)

    for(int i = 1; i <= 100; i++){
        printf("%d\n", i);
    }
    return 0;
}