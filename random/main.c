#include <stdio.h>

int main(){

    // Pseudo-random = Appear random but are determined by a mathematical formula
    //                 that uses a seed value to generate a predicatable sequence 
    //                 of numbers. 

    return 0;
}